###
Welcome to AssetHost
###

# stub console.log() for IE
if !window.console
    class window.console
        @log: ->

AssetHost ?= {}
AssetHost.SERVER = "<%= ASSET_SERVER %>"
AssetHost.BCOVE_PLAYERKEY = "<%= AssetHostSecrets[:brightcove_playerkey]"
AssetHost.BCOVE_PLAYERID = "<%= AssetHostSecrets[:brightcove_playid]"